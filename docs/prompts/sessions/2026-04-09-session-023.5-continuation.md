# Ferratomic Continuation — Session 023.5

> Generated: 2026-04-09
> Last commit: `4bed0d8` "docs(AGENTS,ideas/014): codify Knowledge Organization Rule + status banner"
> Branch: main (synced with master)

## Read First

1. `QUICKSTART.md` — project orientation (updated 2026-04-09)
2. `AGENTS.md` — guidelines and constraints. **NEW since session 023**: §"Decision Framework: GOALS.md §7" + §"Knowledge Organization Rule"
3. `GOALS.md` §7 — **CANONICAL**: The Six-Dimension Decision Evaluation Framework (Performance, **Efficiency**, Accretiveness, Correctness Tier 1, Quality, Optimality). Read in full — load-bearing for this and all future sessions
4. `spec/README.md` — load only the spec modules you need; spec/06 is the target this session
5. `docs/ideas/014-the-alien-stack.md` — exploratory synthesis. Read the **status banner** first (cross-reference table), then §9.12 (dispatch correction), §9.13 (decisions D1-D6), §9.14 (concrete deliverable spec for INV-FERR-045c)
6. `spec/01-core-invariants.md` INV-FERR-001 — gold standard template; calibrate quality expectations against it before authoring
7. `docs/prompts/lifecycle/16-spec-authoring.md` — spec authoring protocol

## Session Summary

### Completed (session 023 + alien stack deep dive, 2026-04-09)

- **Pattern H DEFINITIVELY RESOLVED at the spec layer**: §23.9.0 "Canonical Datom Key Encoding" (267 lines) + INV-FERR-045a "Deterministic Chunk Serialization" (637 lines) authored in spec/06. INV count 86→87. Commit `7aafb9f`.
- **Spec audit Section 7 complete** (lifecycle/17 Phases 1-5): 7 findings, 1 CRITICAL + 2 MAJOR fixed inline. FINDING-226 (INV-FERR-049 multi-tree manifest L2 rewrite), FINDING-227/228 (Lean tautology fixes for INV-FERR-045/046). 5 follow-up Lean concretization beads filed: bd-aqg9h, bd-uhjj3, bd-dhv31, bd-4o8uv, bd-e2gu3.
- **bd-r2u Postcondition #5 addressed**: Root parameter scope disambiguation notes added to INV-FERR-047/048.
- **docs/ideas/014 alien stack synthesis** (~2700 lines): comprehensive deep-dive document covering trait-based DI architecture, four compound stacks (Density/Speed/Federation/Algebraic Query), all alien artifact tiers (1, Ω, ΩΩ, ΩΩΩ), Path-to-10.0 scoring framework + decision history, SOC connection (cautiously framed as speculative). Status banner cross-references all prescriptive content to canonical sources. Commits `b288656` + `b90b77a`.
- **30 alien stack research beads filed** with lab-grade descriptions (source pointers, cass search hints, mathematical foundations, quantitative impact, implementation references, risk profiles, acceptance criteria). Plus 8 fail-fast experiment beads (per doc 013 §IX discipline).
- **Six-Dimension Decision Framework codified to GOALS.md §7** (canonical): Performance, **Efficiency**, Accretiveness, Correctness (Tier 1), Quality, Optimality. Each scored 1-10. Literal 10.0 requires all six at 10.0. Worked example (INV-FERR-049 scoring 7.08 → 9.25). Commit `0c0d326`.
- **Knowledge Organization Rule codified to AGENTS.md** (canonical): prescriptive content goes in canonical sources (spec/, beads, GOALS.md, AGENTS.md, README.md), never in docs/ideas/. Commit `4bed0d8`.
- **README.md §6** (Design Philosophy): framework surfaced for new readers.
- **docs/ideas/014 status banner**: cross-reference table mapping every prescriptive element to its canonical source.
- Memory file `~/.claude/projects/-data-projects-ddis-ferratomic/memory/session023_alien_stack.md` saved with full session capture.

### Decisions Made (locked, see docs/ideas/014 §9.13 for full rationale)

- **D1**: Invariant ID for the conformance trait = `INV-FERR-045c`. Sequence: 045 base → 045a DatomPair reference → 045c trait.
- **D2**: **Enum dispatch** (closed-world `LeafChunk` enum), not static generic, not trait object. Matches Phase 4a `AdaptiveIndexes` precedent. Zero vtable overhead. Mixed-codec stores supported. User pushed back on my initial static-generic recommendation: "doesn't dynamic cohere more closely with the entire DI idea?" — and was right.
- **D3**: Conformance testing scope = BOTH trait-level shared tests + per-codec strengthenings. Matches INV-FERR-025 backend test pattern.
- **D4**: Codec discriminator registry lives in NEW §23.9.8 in spec/06. Normative (not informational). Reserved tags 0x01-0x7F (spec-registered), 0x80-0xFF (experimental).
- **D5**: Session 023.7 (byte-level Lean concretization precedent) IS in scope. Required for literal 10.0.
- **D6**: Commits ADDITIVE to session 023's `7aafb9f`. No rebase. Preserves git history showing design evolution.

**Architectural framing corrections** (also locked):
- **Accretiveness is forward-looking, not backward-looking** (GOALS.md §7.2). A correction that fixes a wrong design is HIGHLY accretive (eliminates future debt). The session 023 INV-FERR-049 rewrite is the canonical example.
- **Performance and Efficiency are distinct dimensions** (GOALS.md §7.1, §7.2). For ferratomic, storage efficiency is a top priority — wavelet matrix ~5 b/d, prolly tree structural sharing, homomorphic fingerprint (32 bytes regardless of store size).
- **SOC (Self-Organized Criticality) from doc 010 §6.4 is SPECULATIVE**, not load-bearing. The alien stack's value is independent of SOC validity. `bd-imwb` empirically validates or refutes.
- **Knowledge Organization Rule** (AGENTS.md): prescriptive content always lives in canonical sources. docs/ideas/ is exploratory only.

### Bugs Found

None during session 023 + deep dive. Pattern H was a documentation gap (phantom citations), not a bug. The 7 findings from the spec audit were quality issues, not defects in shipped behavior.

### Stopping Point

**Exactly where I stopped**: All deep-dive synthesis work is committed and pushed (commits `7aafb9f`, `b288656`, `b90b77a`, `0c0d326`, `4bed0d8`). Working tree is clean, `main` and `master` are synchronized. The next action is to begin **session 023.5 Phase 1**: author `INV-FERR-045c "Leaf Chunk Codec Conformance"` in `spec/06-prolly-tree.md`.

**The last thing I verified working**: `git status` shows clean working tree; `git log --oneline -5` shows the 5 commits from this session sequence; pre-commit gates passed on every commit.

**The next thing to do**: Open `spec/06-prolly-tree.md`, locate the boundary between INV-FERR-045 (ends ~line 555) and INV-FERR-045a (starts ~line 558), and insert the new INV-FERR-045c before INV-FERR-045a. Author per the gold standard INV-FERR-001 template + the specific deliverable spec in `docs/ideas/014` §9.14. Per D6, this is an additive commit; do not rebase.

## Next Execution Scope

### Primary Task

**Author `INV-FERR-045c "Leaf Chunk Codec Conformance"`** in `spec/06-prolly-tree.md`. ~450 lines, ~1.5-2 hours of focused authoring.

This is the load-bearing accretive lever: every alien artifact in `docs/ideas/014` plugs into ferratomic via this single trait. Without it, future codec innovations are scattered bolt-ons. With it, every future innovation is a 1-line enum variant addition that compounds with all previous work.

**No bead ID** — INV-FERR-045c is spec authoring, not a bead-tracked work item. The audit doc and the synthesis doc together provide the specification for what to author. The Phase A plan in docs/ideas/014 §9.14 is the most concrete spec.

**Acceptance criteria for session 023.5 Phase 1** (just authoring INV-FERR-045c):

1. New invariant `INV-FERR-045c "Leaf Chunk Codec Conformance"` authored in `spec/06-prolly-tree.md`, positioned BEFORE `INV-FERR-045a` in linear reading order so the trait contract precedes the reference implementation.
2. All 6 verification layers populated per the gold standard `INV-FERR-001` template:
   - **Header**: Traces to (INV-FERR-005, 025, 045, 046, 074, 086, ADR-FERR-032), Referenced by (045a, 047, 048, 049, gvil.1), Verification (V:PROP, V:KANI, V:TYPE, V:LEAN), Stage 1.
   - **Level 0 (Algebraic Law)**: 5 conformance theorems with proof sketches that cite NAMED MECHANISMS (no "by construction" or "obvious"). Theorems: round-trip, determinism, injectivity, fingerprint homomorphism, order independence.
   - **Level 1 (State Invariant)**: Operational meaning + enum dispatch rationale (per D2) + forward reference to §23.9.8 codec discriminator registry.
   - **Level 2 (Implementation Contract)**: `LeafChunkCodec` trait definition with all method signatures + `boundary_key` default + `CODEC_TAG` associated constant. `LeafChunk` enum with `DatomPair` variant (the only one initially; future variants added via spec evolution). Conformance test harness skeleton. Match-based dispatch in the enum's helper methods.
   - **Falsification**: 5 specific witness types (one per conformance property).
   - **proptest strategy**: 5 proptest strategies (one per property), parameterized over the codec type.
   - **Lean theorem**: Trait-level theorems for the 5 properties, modeled abstractly over `Finset Datom → Bytes`. Use INV-FERR-086 pattern (substantive theorem statement + `sorry` proof body for byte-level claims) with tracked beads for concretization.
3. Cross-references include "Referenced by" pointing to INV-FERR-045a (which becomes DatomPair reference codec in Phase 2), 047, 048, 049, and gvil.1 (spec/09).
4. Score the new invariant across the Six-Dimension framework (GOALS.md §7) and document the scores in the commit message.
5. Pre-commit gates pass (this is a spec edit; cargo gates run but no Rust code changes).
6. Commit + push to main + master.
7. Continuation handoff prompt for Phase 2 written if Phase 1 ends without completing all of session 023.5.

### Ready Queue

```bash
br ready          # Show unblocked issues
bv --robot-next   # Top pick with reasoning
```

**Note**: The 30 alien stack beads filed in this session are all P1-P4 research backlog. They are intentionally NOT actionable until the trait architecture is in place. After session 023.5 ships INV-FERR-045c, these beads become actionable for parallel implementation tracks (Phase C in `docs/ideas/014` §9.3).

### Dependency Context

- **Session 023.5 Phase 1** (this task) blocks all subsequent Phase A sessions.
- **Phase 2** of session 023.5 (refactor INV-FERR-045a as DatomPair reference codec) depends on Phase 1.
- **Phases 3-7** of session 023.5 (§23.9.0 trait-aware updates, bd-4vwk acceptance, helper definitions, codec registry §23.9.8, performance budgets, commit) depend on Phases 1-2.
- **Session 023.5.5** (trait edge-case hardening) depends on session 023.5 completion.
- **Session 023.6** (DiffIterator body + Lean for 047/048/050 + budgets) depends on 023.5.5.
- **Session 023.6.5** (Tier 1 inline integration: BP+RMM, sendfile, IBLT) depends on 023.6.
- **Session 023.7** (byte-level Lean concretization precedent) depends on 023.6.5.

The 30 alien stack research beads are NOT in the dependency chain — they are backlog for parallel Phase C tracks after Phase A completes.

## Hard Constraints

- Safe callable surface: `#![forbid(unsafe_code)]` by default; internal unsafe permitted only when firewalled behind safe APIs, mission-critical, and ADR-documented (GOALS.md §6.2)
- No `unwrap()` in production code (NEG-FERR-001)
- `CARGO_TARGET_DIR=/data/cargo-target` (MUST set; default uses /tmp which is RAM-backed)
- Phase N+1 cannot start until Phase N passes isomorphism check
- Full defensive engineering standards: GOALS.md §6
- **NEW**: Six-Dimension Decision Framework (GOALS.md §7) consulted before non-trivial decisions; score the work and document in commit messages
- **NEW**: Knowledge Organization Rule (AGENTS.md) enforced — prescriptive content goes in canonical sources, NOT in docs/ideas/
- NO subagent delegation for spec authoring
- NO batch updates — sequential, one element at a time
- Read primary sources every time — every spec citation grep-verified
- NO worktree isolation
- DO NOT modify audit doc §16-20 (locked decisions)
- DO NOT touch INV-FERR-045 (Chunk Content Addressing) — content is unchanged
- DO NOT touch the Snapshot rewrite from session 023 — that's the FINDING-226 fix, locked
- DO NOT confuse codec-level concerns (per-chunk format, per-codec optimization) with trait-level concerns (the 5 conformance properties, the LeafChunk enum dispatch)

## Stop Conditions

Stop and escalate to the user if:

- The trait design has a hole that requires more than 5 conformance properties (5 was carefully chosen; a 6th suggests the abstraction is wrong)
- Static generic dispatch starts looking attractive again (it's not — see docs/ideas/014 §9.12; enum dispatch is locked per D2)
- A canonical source (spec/, GOALS.md, AGENTS.md, README.md) contradicts a prescriptive claim in docs/ideas/014 (the canonical source wins; update docs/ideas/014 to reference the canonical source as authoritative)
- An alien artifact appears to compromise the algebraic foundation `(P(D), ∪)` (the foundation is non-negotiable per GOALS.md §3 Tier 1)
- You discover a prescriptive statement in docs/ideas/* with no canonical source listed (this is a documentation defect per the Knowledge Organization Rule; either elevate it or reframe as exploratory)
- Any cargo gate fails during the spec edit work (unlikely but possible if helper types are inconsistent)
- The composite Six-Dimension score after authoring INV-FERR-045c is below 8.5 (regroup; figure out which dimension is below the bar and why)

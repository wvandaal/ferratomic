# Path to 10.0 / A+ — Zero-Defect Cleanroom Standard

> **Date**: 2026-04-01
> **Baseline**: B+ (8.52), regressed from A- (8.82)
> **Target**: 10.0 / A+ across all 10 vectors
> **Standard**: Lab-grade. Zero defects. NASA-grade Rust. No "close enough."
>
> A 10.0 means: every invariant proven at maximum feasible depth, every quality gate
> passes without exception, every public API documented with invariant references,
> every architectural decision justified and verified, every performance target
> asserted mechanically, every phase gate formally closed with evidence.

---

## Current Scores and Exact Deltas to 10.0

| # | Vector | Weight | Current | Target | Delta | Blocking? |
|---|--------|--------|---------|--------|-------|-----------|
| 1 | Correctness | 3x | 9.1 | 10.0 | +0.9 | No |
| 2 | Completeness | 2x | 8.5 | 10.0 | +1.5 | No |
| 3 | Verification Depth | 2x | 8.8 | 10.0 | +1.2 | No |
| 4 | Code Quality | 1.5x | 6.8 | 10.0 | +3.2 | **YES** |
| 5 | Architecture | 1.5x | 9.2 | 10.0 | +0.8 | No |
| 6 | Performance | 1.5x | 7.5 | 10.0 | +2.5 | No |
| 7 | Durability | 2x | 9.0 | 10.0 | +1.0 | No |
| 8 | Ergonomics | 0.5x | 8.3 | 10.0 | +1.7 | No |
| 9 | Axiological Alignment | 2x | 9.3 | 10.0 | +0.7 | No |
| 10 | Process Health | 1x | 8.0 | 10.0 | +2.0 | **YES** |

Composite to reach 10.0: every vector must score 10.0. There is no averaging trick.

---

## TIER 0: STOP THE BLEEDING (Blocks everything else)

These must be fixed before ANY other work. The build is broken.

### T0-001: Fix clippy unnecessary_wraps in production code
- **File**: `ferratomic-core/src/db/transact.rs:146`
- **Problem**: `publish_and_check(&self, new_store: Store) -> Result<(), FerraError>` always returns `Ok(())`
- **Fix**: Change return type to `()`, remove `Ok(())` at line 169
- **Blocks**: Strict gate (`cargo clippy --workspace --lib -- -D warnings`)
- **Effort**: 2 minutes

### T0-002: Fix unused imports in test code
- **File**: `ferratomic-core/src/db/tests.rs:1,3,6`
- **Problem**: `atomic::Ordering`, `FerraError`, `indexes::Indexes` imported but unused
- **Fix**: Remove the three unused imports
- **Blocks**: All-targets gate (`cargo clippy --workspace --all-targets -- -D warnings`)
- **Effort**: 1 minute

### T0-003: Fix borrow-after-partial-move in integration test
- **File**: `ferratomic-verify/integration/test_schema.rs:212,256`
- **Problem**: `SchemaViolation { got, .. }` moves `got: String`, then `{result:?}` borrows `result`
- **Fix**: Change to `SchemaViolation { ref got, .. }` at both locations
- **Blocks**: Test compilation
- **Effort**: 1 minute

### T0-004: Fix cloned_ref_to_slice_refs lint
- **Problem**: New clippy lint `cloned_ref_to_slice_refs` triggered somewhere in workspace
- **Fix**: Find source with `cargo clippy --workspace --all-targets -- -D warnings 2>&1 | grep cloned_ref`, fix the usage (use slice directly instead of cloning a reference)
- **Blocks**: All-targets gate
- **Effort**: 5 minutes

### T0-005: Verify all gates pass after fixes
- **Commands**:
  ```bash
  export CARGO_TARGET_DIR=/data/cargo-target
  cargo check --workspace --all-targets
  cargo clippy --workspace --all-targets -- -D warnings
  cargo clippy --workspace --lib -- -D warnings -D clippy::unwrap_used -D clippy::expect_used -D clippy::panic
  cargo fmt --all -- --check
  PROPTEST_CASES=1000 cargo test --workspace
  ```
- **Acceptance**: All 5 commands exit 0
- **Effort**: 10 minutes (compilation time)

**Total Tier 0 effort: 20 minutes. Non-negotiable prerequisite.**

---

## TIER 1: CLOSE THE PHASE 4a GATE (Critical path)

### T1-001: Close bd-lplt (PROC-3: Full regression suite)
- **Action**: Run `PROPTEST_CASES=1000 cargo test --workspace` — all pass
- **Then**: `br close bd-lplt --reason "All 287 tests pass, 0 failures, PROPTEST_CASES=1000"`
- **Effort**: 5 minutes

### T1-002: Close bd-y1w5 (PROC-4: Tag and document)
- **Action**: `git tag v0.4.0-gate -m "Phase 4a gate closure"`
- **Then**: `br close bd-y1w5 --reason "Tagged v0.4.0-gate"`
- **Effort**: 2 minutes

### T1-003: Close bd-add (Phase 4a gate)
- **Action**: `br close bd-add --reason "Phase 4a gate closed. 30/32 INV-FERR implemented, all tests pass, all quality gates clean."`
- **Unblocks**: bd-flqz, bd-keyt, bd-nhui (Phase 4b prerequisites)
- **Effort**: 1 minute

### T1-004: Install pre-commit hook (green-main invariant)
- **File**: `.git/hooks/pre-commit` or `.claude/settings.json` hook
- **Content**: Run fast gate before every commit
  ```bash
  export CARGO_TARGET_DIR=/data/cargo-target
  cargo check --workspace --all-targets && \
  cargo clippy --workspace --all-targets -- -D warnings && \
  cargo fmt --all -- --check
  ```
- **Purpose**: Prevents future regressions — no broken commit ever reaches main
- **Effort**: 5 minutes

**Total Tier 1 effort: 15 minutes after Tier 0.**

---

## TIER 2: CORRECTNESS → 10.0 (Current 9.1)

A 10.0 requires: every Phase 4a INV-FERR proven in Lean with 0 sorry, verified by proptest
at 10K+ cases, bounded-verified by Kani, model-checked by Stateright where applicable,
integration-tested, and type-level enforced where possible. No known gaps. No partial
invariants.

### What's missing for 10.0:

**Lean proof gaps (Phase 4a invariants without Lean theorems):**

| Gap | INV-FERR | What's needed | File | Effort |
|-----|----------|---------------|------|--------|
| T2-001 | 014 (Recovery Correctness) | Lean theorem: `recover(crash(S)) ⊇ last_committed(S)` — model crash as partial WAL, prove superset property | `lean/Ferratomic/Concurrency.lean` | M |
| T2-002 | 019 (Error Exhaustiveness) | Lean theorem: error type cardinality covers all failure modes — model as sum type | `lean/Ferratomic/Refinement.lean` | S |
| T2-003 | 021 (Backpressure Safety) | Lean theorem: bounded queue never drops committed work — model as capacity invariant | `lean/Ferratomic/Concurrency.lean` | S |
| T2-004 | 022 (Anti-Entropy Convergence) | Lean theorem: anti-entropy eventually delivers all datoms — fairness assumption + fixed-point | `lean/Ferratomic/Federation.lean` | M |
| T2-005 | 023 (No Unsafe Code) | Lean axiom: unsafe_code = False (axiomatic, mirrors forbid attribute) | `lean/Ferratomic/Refinement.lean` | S |
| T2-006 | 024 (Substrate Agnosticism) | Lean theorem: backend-parameterized operations preserve store identity | `lean/Ferratomic/Performance.lean` | S |
| T2-007 | 025 (Index Backend Interchangeability) | Lean theorem: IndexBackend trait preserves bijection | `lean/Ferratomic/Performance.lean` | S |
| T2-008 | 026 (Write Amplification) | Not provable in Lean (empirical). Mark as N/A-LEAN in catalog. | — | — |
| T2-009 | 027 (Read Latency) | Not provable in Lean (empirical). Mark as N/A-LEAN in catalog. | — | — |
| T2-010 | 028 (Cold Start) | Not provable in Lean (empirical). Mark as N/A-LEAN in catalog. | — | — |
| T2-011 | 030 (Replica Filter) | Lean theorem: AcceptAll ≡ identity filter, filter preserves subset property | `lean/Ferratomic/Federation.lean` | S |

**Kani gaps (Phase 4a invariants without Kani harnesses):**

| Gap | INV-FERR | What's needed | File | Effort |
|-----|----------|---------------|------|--------|
| T2-012 | 022 (Anti-Entropy) | Kani harness: NullAntiEntropy diff + apply_diff roundtrip | `kani/anti_entropy.rs` | S |
| T2-013 | 023 (No Unsafe Code) | Kani harness: meta-verification (read lib.rs, assert forbid) | `kani/safety.rs` | S |
| T2-014 | 024 (Substrate Agnosticism) | Kani harness: InMemoryBackend vs FsBackend produce identical Store | `kani/substrate.rs` | S |
| T2-015 | 025 (Index Backend) | Kani harness: OrdMap insert/get roundtrip across all 4 index types | `kani/index_backend.rs` | S |
| T2-016 | 026 (Write Amplification) | Kani harness: bounded WAL frame size (≤2KB per datom) | `kani/write_amp.rs` | S |
| T2-017 | 027 (Read Latency) | Not feasible in Kani (timing). Mark as N/A-KANI. | — | — |
| T2-018 | 028 (Cold Start) | Not feasible in Kani (timing). Mark as N/A-KANI. | — | — |
| T2-019 | 030 (Replica Filter) | Kani harness: AcceptAll.accepts() always true | `kani/replica_filter.rs` | S |

**CI-FERR operationalization:**

| Gap | INV | What's needed | File | Effort |
|-----|-----|---------------|------|--------|
| T2-020 | CI-FERR-001 | proptest conformance: Lean predictions matched by Rust output for known test vectors | `proptest/conformance.rs` | M |
| T2-021 | CI-FERR-002 | Verify type refinement tower: EntityId cardinality = 2^256, Value = sum of 11, Op = 2, Datom = product, Transaction<Committed> = sealed | `proptest/refinement.rs` | S |

**Complete INV-FERR-010 and 017 (currently "partial"):**

| Gap | INV | What's needed | Effort |
|-----|-----|---------------|--------|
| T2-022 | 010 (Convergence) | Add explicit convergence assertion in `store/merge.rs` — verify merge(A,B) == merge(B,A) at datom level, not just set level | S |
| T2-023 | 017 (Shard Equivalence) | Add explicit shard union == full store assertion in `indexes.rs` or integration test | S |

**Total Tier 2: 23 items. Effort: 4-5 sessions.**

---

## TIER 3: VERIFICATION DEPTH → 10.0 (Current 8.8)

A 10.0 requires: every Phase 4a INV-FERR has at least 4 independent verification layers.
Performance invariants (026-028) get proptest + integration + threshold assertion (3 layers
is maximum feasible — Lean and Kani don't do timing). All other invariants get 4+.

### Stateright model gaps:

Currently 6 models cover 10 invariants. For 10.0, every concurrency/convergence invariant
needs a Stateright model.

| Gap | INV-FERR | Model needed | File | Effort |
|-----|----------|-------------|------|--------|
| T3-001 | 004 (Monotonic Growth) | Model: apply_tx never shrinks datom set | `stateright/monotonic_model.rs` | S |
| T3-002 | 005 (Index Bijection) | Model: 4 indexes always agree after any action sequence | `stateright/bijection_model.rs` | M |
| T3-003 | 008 (WAL Ordering) | Model: fsync before swap — writer/reader interleaving model | `stateright/wal_ordering_model.rs` | M |
| T3-004 | 009 (Schema Validation) | Model: invalid datoms always rejected at transact boundary | `stateright/schema_model.rs` | S |
| T3-005 | 011 (Observer Monotonicity) | Model: observer epoch never decreases across notifications | `stateright/observer_model.rs` | S |
| T3-006 | 013 (Checkpoint Equivalence) | Model: serialize-deserialize roundtrip preserves state | `stateright/checkpoint_model.rs` | S |
| T3-007 | 015 (HLC Monotonicity) | Model: tick() always produces strictly greater TxId | `stateright/hlc_model.rs` | S |
| T3-008 | 018 (Append-Only) | Model: store size monotonically non-decreasing | `stateright/append_only_model.rs` | S |

### Integration test gaps:

Every Phase 4a INV-FERR must have a named integration test. Currently 32 tests cover
most, but some have only indirect coverage.

| Gap | INV-FERR | Test needed | File | Effort |
|-----|----------|------------|------|--------|
| T3-009 | 022 (Anti-Entropy) | E2E: two stores, apply NullAntiEntropy, verify no-op | `integration/test_anti_entropy.rs` | S |
| T3-010 | 024 (Substrate Agnosticism) | E2E: InMemoryBackend + FsBackend produce identical state after same txns | `integration/test_substrate.rs` | S |
| T3-011 | 025 (Index Backend) | E2E: OrdMap backend insert/get/range-scan correctness | `integration/test_index_backend.rs` | S |
| T3-012 | 030 (Replica Filter) | E2E: AcceptAll.accepts() for all datom variants | `integration/test_replica_filter.rs` | S |

### Proptest gap fill:

All Phase 4a INV-FERR already have proptest coverage. For 10.0, raise case count and
add regression seeds for edge cases found during development.

| Gap | Action | File | Effort |
|-----|--------|------|--------|
| T3-013 | Add proptest regression seeds from all `.proptest-regressions` files into version control | `proptest/*.proptest-regressions` | S |
| T3-014 | Add explicit `PROPTEST_CASES=10000` annotation to every proptest that currently defaults | All `proptest/*.rs` files | S |

### Type-level enforcement:

| Gap | INV-FERR | Enforcement | File | Effort |
|-----|----------|------------|------|--------|
| T3-015 | 023 (No Unsafe) | Already enforced: `#![forbid(unsafe_code)]` in all 4 crates. Document in catalog as "Type-Level: Y (compiler-enforced)" | `invariant_catalog.rs` | S |
| T3-016 | 018 (Append-Only) | Already enforced: Datom has no `&mut self` methods, EntityId has no setters. Document as type-level | `invariant_catalog.rs` | S |
| T3-017 | 012 (Content Identity) | Already enforced: EntityId(`[u8; 32]`) private field, only `from_content()` public constructor. Document | `invariant_catalog.rs` | S |
| T3-018 | 006 (Snapshot Isolation) | Already enforced: ArcSwap<Store> + im::OrdSet structural sharing = O(1) clone. Document | `invariant_catalog.rs` | S |

**Total Tier 3: 18 items. Effort: 3-4 sessions.**

---

## TIER 4: CODE QUALITY → 10.0 (Current 6.8)

A 10.0 requires: all 5 quality gates pass on every commit. Zero clippy warnings on any
target. Zero `unwrap()` or `expect()` in production code. Zero suppressions. Every file
under 500 LOC. Every function under 50 LOC. Every function with ≤5 parameters. Every
public item documented with INV-FERR references. `#![forbid(unsafe_code)]` and
`#![deny(missing_docs, clippy::all)]` in all crates.

### Current defects (from code quality audit):

| Gap | Issue | Location | Fix | Effort |
|-----|-------|----------|-----|--------|
| T4-001 | `publish_and_check` unnecessary_wraps | `db/transact.rs:146` | Return `()` instead of `Result<(), FerraError>` | S |
| T4-002 | Unused imports | `db/tests.rs:1,3,6` | Remove 3 imports | S |
| T4-003 | Borrow-after-move | `test_schema.rs:212,256` | Add `ref` to destructuring | S |
| T4-004 | `cloned_ref_to_slice_refs` | TBD (find via clippy) | Fix source | S |

**(These are identical to Tier 0 — listed here for completeness.)**

### Structural quality for 10.0:

| Gap | Check | Current | Action | Effort |
|-----|-------|---------|--------|--------|
| T4-005 | All 4 crates have `#![forbid(unsafe_code)]` | YES | Verify (already passing) | — |
| T4-006 | All 4 crates have `#![deny(missing_docs)]` | YES | Verify (already passing) | — |
| T4-007 | All 4 crates have `#![deny(clippy::all)]` | YES | Verify (already passing) | — |
| T4-008 | Zero `#[allow(...)]` suppressions | YES (0 found) | Verify (already passing) | — |
| T4-009 | Zero `unwrap()` in production code | YES (all in `#[cfg(test)]`) | Verify strict gate passes | — |
| T4-010 | Zero `expect()` in production code | YES (all in `#[cfg(test)]`) | Verify strict gate passes | — |
| T4-011 | Zero `panic!`/`todo!`/`unimplemented!` in production | YES (all in tests) | Verify strict gate passes | — |
| T4-012 | Zero `eprintln!`/`println!`/`dbg!` in production | YES (0 found) | Already clean | — |
| T4-013 | All production files < 500 LOC | YES (max 438, store/apply.rs) | Already clean | — |
| T4-014 | All functions < 50 LOC | YES (audit confirmed) | Already clean | — |
| T4-015 | All functions ≤ 5 parameters | YES (audit confirmed) | Already clean | — |
| T4-016 | 3 TODOs in ferratomic-datalog | Phase 4d stubs | Acceptable — documented with bead IDs | — |

### Pin Rust toolchain:

| Gap | Action | File | Effort |
|-----|--------|------|--------|
| T4-017 | Create `rust-toolchain.toml` pinning nightly date | `rust-toolchain.toml` | S |
| T4-018 | Set `channel = "nightly-2026-02-20"` (matches current rustc) | `rust-toolchain.toml` | S |

This prevents surprise clippy lint additions from breaking the build.

**Total Tier 4: 6 real actions (T4-001..004 = Tier 0, T4-017..018 new). Effort: S.**

---

## TIER 5: PERFORMANCE → 10.0 (Current 7.5)

A 10.0 requires: all spec performance targets mechanically asserted. No O(n) operations
hidden inside O(1) interfaces. Benchmarks exist and pass. Known limitations tracked and
accepted with ADR justification.

### Performance assertion gaps:

| Gap | INV-FERR | What's needed | File | Effort |
|-----|----------|---------------|------|--------|
| T5-001 | 026 (Write Amplification < 10x) | Add `#[test]` asserting WAL frame size / datom logical size < 10.0 for 100, 1K, 10K datoms | `integration/test_thresholds.rs` | S |
| T5-002 | 027 (Read P99 < 1ms) | Add `#[test]` timing EAVT point lookup P99 across 1K, 10K, 25K datoms — assert < 1_000_000 ns | `integration/test_thresholds.rs` | S |
| T5-003 | 028 (Cold Start < 5s) | Add `#[test]` timing checkpoint load + WAL replay for 1K, 5K datoms — assert < 5.0s | `integration/test_thresholds.rs` | S |

*(Note: test_thresholds.rs already has some threshold tests. These may need strengthening.)*

### Known O(n) operations — document and justify:

| Gap | Operation | Location | Justification | Action |
|-----|-----------|----------|---------------|--------|
| T5-004 | Index rebuild during merge | `store/merge.rs` | Merge is O(\|A\|+\|B\|) by nature (set union). Index rebuild is O(n) but occurs inside an O(n) operation. Acceptable. | Add comment citing ADR-FERR-001 |
| T5-005 | WAL reads entire file | `wal/recover.rs` | Recovery is O(n) by nature. Streaming deferred to Phase 4b. | Add comment citing INV-FERR-028 + Phase 4b bead |
| T5-006 | Observer O(n) catchup | `observer.rs` | Bounded ring buffer normally avoids this. O(n) only when buffer exhausted. | Add comment citing HI-012 + Phase 4b bead |
| T5-007 | CRC32 bit-by-bit | `wal/writer.rs` | Correct but ~10x slower than table-based. Acceptable for Phase 4a frame sizes. | Add comment citing Phase 4b optimization bead |
| T5-008 | Checkpoint serialization O(n) | `checkpoint.rs` | Unavoidable for full-store checkpoint. Phase 4b prolly tree provides O(d) incremental. | Add comment citing ADR-FERR-008 |

### Benchmark hardening:

| Gap | Action | File | Effort |
|-----|--------|------|--------|
| T5-009 | Add criterion benchmark for merge of two 10K-datom stores | `benches/merge_benchmark.rs` | S |
| T5-010 | Add criterion benchmark for checkpoint round-trip at 1K, 10K datoms | `benches/checkpoint_benchmark.rs` | S |
| T5-011 | Add criterion benchmark for EAVT range scan (100 results from 10K store) | `benches/index_benchmark.rs` | S |

**Total Tier 5: 11 items. Effort: 2 sessions.**

---

## TIER 6: DURABILITY → 10.0 (Current 9.0)

A 10.0 requires: crash recovery verified under adversarial fault injection, multi-crash
scenarios tested, WAL ordering mechanically proven, checkpoint integrity asserted.

| Gap | What's needed | File | Effort |
|-----|---------------|------|--------|
| T6-001 | Double-crash recovery integration test: genesis → tx3 → checkpoint → tx2 → crash → recover → tx1 → crash → recover — verify final state correct | `integration/test_recovery.rs` | S |
| T6-002 | Triple-crash with WAL truncation: genesis → tx5 → crash (partial WAL frame) → recover → tx3 → crash → recover — verify truncated frames discarded | `integration/test_recovery.rs` | S |
| T6-003 | Bit-flip detection: corrupt 1 byte in checkpoint payload, verify BLAKE3 rejects | `integration/test_recovery.rs` | S (may already exist — verify) |
| T6-004 | Power-cut simulation: write checkpoint, kill mid-rename, verify old checkpoint survives (atomic write via temp+rename) | `proptest/fault_recovery_properties.rs` | S |
| T6-005 | Disk-full simulation: WAL append fails with ENOSPC, verify committed data survives, new txn returns Io error | `proptest/fault_recovery_properties.rs` | S |
| T6-006 | Concurrent crash+recovery: two threads — one writing WAL, one triggering crash — verify no data loss | `integration/test_recovery.rs` | M |

**Total Tier 6: 6 items. Effort: 1-2 sessions.**

---

## TIER 7: COMPLETENESS → 10.0 (Current 8.5)

A 10.0 requires: 32/32 Phase 4a INV-FERR fully implemented and verified. INV-FERR-010
and 017 not "partial." All Phase 4b-4d invariants formally specified in spec (already true).

| Gap | INV-FERR | Action | File | Effort |
|-----|----------|--------|------|--------|
| T7-001 | 010 (Convergence) | Add explicit convergence integration test: 3 replicas, 3 different merge orderings, assert identical final state at datom level (not just set level) | `integration/test_crdt.rs` | S |
| T7-002 | 017 (Shard Equivalence) | Add explicit shard-union integration test: partition by entity hash, verify union == original, verify shards disjoint | `integration/test_crdt.rs` | S |
| T7-003 | Mark 010/017 as COMPLETE in invariant_catalog.rs after tests pass | `invariant_catalog.rs` | S |

**Total Tier 7: 3 items. Effort: S (30 minutes).**

---

## TIER 8: ARCHITECTURE → 10.0 (Current 9.2)

A 10.0 requires: crate DAG acyclic (already), every module has one concept (already),
public API minimal (already), trait contracts formally documented, wire/core boundary
provably enforced, no God modules, no circular dependencies.

| Gap | What's needed | File | Effort |
|-----|---------------|------|--------|
| T8-001 | Document IndexBackend trait contract: insert/get roundtrip, cardinality preservation, ordering guarantee | `ferratomic-core/src/indexes.rs` doc comments | S |
| T8-002 | Document StorageBackend trait contract: checkpoint atomicity, WAL append-only, backend interchangeability | `ferratomic-core/src/storage/mod.rs` doc comments | S |
| T8-003 | Document DatomObserver trait contract: monotonic epoch delivery, at-least-once semantics, bounded replay | `ferratomic-core/src/observer.rs` doc comments | S |
| T8-004 | Document AntiEntropy trait contract: eventual convergence, idempotent apply | `ferratomic-core/src/anti_entropy.rs` doc comments | S |
| T8-005 | Document ReplicaFilter trait contract: subset preservation, filter composability | `ferratomic-core/src/topology.rs` doc comments | S |
| T8-006 | Verify public API surface is minimal: no internal types exposed through public methods | Audit `lib.rs` re-exports in all 4 crates | S |
| T8-007 | Verify wire/core type boundary: no path from `Deserialize` to core `Datom`/`EntityId`/`Value` without `into_trusted()` | Audit `ferratom/src/wire.rs` + `ferratom/src/datom/` | S |

**Total Tier 8: 7 items. Effort: 1 session (documentation).**

---

## TIER 9: ERGONOMICS → 10.0 (Current 8.3)

A 10.0 requires: typestate enforced everywhere (already), errors actionable with
category-based recovery guidance (mostly), API surface intuitive with compile-time
prevention of misuse.

| Gap | What's needed | File | Effort |
|-----|---------------|------|--------|
| T9-001 | Add recovery guidance to each FerraError variant doc comment: "Retryable: yes/no", "Caller action: ..." | `ferratom/src/error.rs` | S |
| T9-002 | Add usage example in crate-level doc: open → transact → snapshot → query → checkpoint → close | `ferratomic-core/src/lib.rs` module doc | S |
| T9-003 | Add usage example for schema evolution: define attribute → transact schema datoms → use new attribute | `ferratomic-core/src/schema_evolution.rs` doc | S |
| T9-004 | Add usage example for merge: two independent stores → merge → verify convergence | `ferratomic-core/src/merge.rs` doc | S |
| T9-005 | Add usage example for WAL recovery: cold_start → verify state | `ferratomic-core/src/storage/mod.rs` doc | S |
| T9-006 | Verify FerraError::Io preserves ErrorKind (currently loses it per HI-017) — either fix or document as accepted limitation with ADR | `ferratom/src/error.rs` | S |

**Total Tier 9: 6 items. Effort: 1 session (documentation).**

---

## TIER 10: AXIOLOGICAL ALIGNMENT → 10.0 (Current 9.3)

A 10.0 requires: every line of code traces to a named INV-FERR, ADR-FERR, NEG-FERR,
or constraint. No ungrounded code. No speculative features. Value hierarchy
(GOALS.md) resolves all tradeoffs.

| Gap | What's needed | File | Effort |
|-----|---------------|------|--------|
| T10-001 | Audit: grep all source files for functions without INV-FERR/ADR-FERR reference in doc comment. Add references or file gap beads. | All `src/*.rs` | M |
| T10-002 | Verify CI-FERR-001 correspondence: Lean theorem names match Rust test names for all Phase 4a invariants | Cross-reference `lean/*.lean` with `proptest/*.rs` + `integration/*.rs` | S |
| T10-003 | Verify CI-FERR-002 type refinement: each Curry-Howard encoding in spec matches Rust type definition | Cross-reference `spec/07-refinement.md` with `ferratom/src/` | S |
| T10-004 | Verify GOALS.md value hierarchy applied to all ADR-FERR decisions: each decision references the value tier that resolved it | Cross-reference `GOALS.md` with spec ADR sections | S |

**Total Tier 10: 4 items. Effort: 1 session.**

---

## TIER 11: PROCESS HEALTH → 10.0 (Current 8.0)

A 10.0 requires: Phase 4a gate formally closed (with tag), all quality gates pass on
every commit, defect count < 5 open Phase 4a bugs, cleanroom review performed and
documented, velocity steady, zero bv alerts, zero graph cycles.

| Gap | What's needed | Action | Effort |
|-----|---------------|--------|--------|
| T11-001 | Phase 4a gate closed | Tier 1 | S |
| T11-002 | Pre-commit hook installed | Tier 1 (T1-004) | S |
| T11-003 | All working tree changes committed atomically | Commit verified changes, discard unrelated | S |
| T11-004 | Open Phase 4a bugs triaged to < 5 | `br list --status=open --label=phase-4a` — close or defer each | S |
| T11-005 | Perform cleanroom review of Phase 4a (using lifecycle/06-cleanroom-review.md) | Full adversarial code audit of ferratomic-core | M |
| T11-006 | Document cleanroom review results | `docs/reviews/2026-04-XX-cleanroom-review-phase4a.md` | S |
| T11-007 | Verify 0 bv alerts | `bv --robot-alerts` exits with 0 alerts | S |
| T11-008 | Verify 0 graph cycles | `bv --robot-insights` shows 0 cycles | S |
| T11-009 | Update invariant_catalog.rs to mark all 32/32 Phase 4a INV-FERR as verified | `ferratomic-verify/src/invariant_catalog.rs` | S |

**Total Tier 11: 9 items. Effort: 2 sessions (cleanroom review is the big one).**

---

## COMPLETE EXECUTION PLAN

### Session 1: STOP THE BLEEDING + GATE CLOSURE (1-2 hours)
- [ ] T0-001: Fix `publish_and_check` return type
- [ ] T0-002: Fix unused imports in `db/tests.rs`
- [ ] T0-003: Fix `ref got` in `test_schema.rs`
- [ ] T0-004: Fix `cloned_ref_to_slice_refs`
- [ ] T0-005: Verify all 5 quality gates pass
- [ ] T1-001: Close bd-lplt (full regression)
- [ ] T1-002: Close bd-y1w5 (tag v0.4.0-gate)
- [ ] T1-003: Close bd-add (Phase 4a gate)
- [ ] T1-004: Install pre-commit hook
- [ ] T4-017: Create rust-toolchain.toml
- [ ] T4-018: Pin nightly-2026-02-20
- [ ] T11-003: Commit all working tree changes

### Session 2: LEAN PROOFS (2-3 hours)
- [ ] T2-001: Lean proof for INV-FERR-014 (recovery correctness)
- [ ] T2-002: Lean proof for INV-FERR-019 (error exhaustiveness)
- [ ] T2-003: Lean proof for INV-FERR-021 (backpressure safety)
- [ ] T2-004: Lean proof for INV-FERR-022 (anti-entropy convergence)
- [ ] T2-005: Lean axiom for INV-FERR-023 (no unsafe)
- [ ] T2-006: Lean proof for INV-FERR-024 (substrate agnosticism)
- [ ] T2-007: Lean proof for INV-FERR-025 (index backend)
- [ ] T2-011: Lean proof for INV-FERR-030 (replica filter)
- [ ] Mark 026/027/028 as N/A-LEAN in catalog

### Session 3: KANI HARNESSES + PROPTEST (2-3 hours)
- [ ] T2-012: Kani harness for INV-FERR-022
- [ ] T2-013: Kani harness for INV-FERR-023
- [ ] T2-014: Kani harness for INV-FERR-024
- [ ] T2-015: Kani harness for INV-FERR-025
- [ ] T2-016: Kani harness for INV-FERR-026
- [ ] T2-019: Kani harness for INV-FERR-030
- [ ] T2-020: proptest conformance for CI-FERR-001
- [ ] T2-021: proptest refinement for CI-FERR-002
- [ ] T3-013: Commit proptest regression seeds
- [ ] T3-014: Set PROPTEST_CASES=10000 explicitly

### Session 4: STATERIGHT MODELS (2-3 hours)
- [ ] T3-001: Stateright model for INV-FERR-004 (monotonic growth)
- [ ] T3-002: Stateright model for INV-FERR-005 (index bijection)
- [ ] T3-003: Stateright model for INV-FERR-008 (WAL ordering)
- [ ] T3-004: Stateright model for INV-FERR-009 (schema validation)
- [ ] T3-005: Stateright model for INV-FERR-011 (observer monotonicity)
- [ ] T3-006: Stateright model for INV-FERR-013 (checkpoint equivalence)
- [ ] T3-007: Stateright model for INV-FERR-015 (HLC monotonicity)
- [ ] T3-008: Stateright model for INV-FERR-018 (append-only)

### Session 5: INTEGRATION TESTS + COMPLETENESS (2-3 hours)
- [ ] T3-009: Integration test for INV-FERR-022 (anti-entropy)
- [ ] T3-010: Integration test for INV-FERR-024 (substrate)
- [ ] T3-011: Integration test for INV-FERR-025 (index backend)
- [ ] T3-012: Integration test for INV-FERR-030 (replica filter)
- [ ] T7-001: Convergence integration test (010)
- [ ] T7-002: Shard equivalence integration test (017)
- [ ] T7-003: Update invariant_catalog.rs
- [ ] T2-022: Complete INV-FERR-010
- [ ] T2-023: Complete INV-FERR-017

### Session 6: DURABILITY + PERFORMANCE (2-3 hours)
- [ ] T6-001: Double-crash recovery test
- [ ] T6-002: Triple-crash + WAL truncation test
- [ ] T6-003: Bit-flip detection test
- [ ] T6-004: Power-cut simulation
- [ ] T6-005: Disk-full simulation
- [ ] T6-006: Concurrent crash+recovery test
- [ ] T5-001: Write amplification threshold assertion
- [ ] T5-002: Read latency threshold assertion
- [ ] T5-003: Cold start threshold assertion
- [ ] T5-004..008: Document O(n) operations with justification
- [ ] T5-009..011: Criterion benchmarks

### Session 7: CLEANROOM REVIEW (2-3 hours)
- [ ] T11-005: Full adversarial cleanroom review of ferratomic-core
- [ ] T11-006: Document review results
- [ ] T11-004: Triage open Phase 4a bugs to < 5
- [ ] T11-007: Verify 0 bv alerts
- [ ] T11-008: Verify 0 graph cycles

### Session 8: DOCUMENTATION + POLISH (2-3 hours)
- [ ] T8-001..005: Trait contract documentation
- [ ] T8-006: Public API surface audit
- [ ] T8-007: Wire/core boundary audit
- [ ] T9-001..006: Ergonomics documentation
- [ ] T10-001..004: Axiological alignment verification
- [ ] T3-015..018: Document type-level enforcement in catalog
- [ ] T11-009: Update invariant_catalog.rs to 32/32

### Session 9: FINAL VERIFICATION + GATE (1-2 hours)
- [ ] Run full verification suite: `PROPTEST_CASES=10000 cargo test --workspace --release`
- [ ] Run strict gate: `cargo clippy --workspace --lib -- -D warnings -D clippy::unwrap_used -D clippy::expect_used -D clippy::panic`
- [ ] Run all-targets gate: `cargo clippy --workspace --all-targets -- -D warnings`
- [ ] Verify Lean: `cd ferratomic-verify/lean && lake build` — 0 sorry
- [ ] Run bv: `bv --robot-triage` — 0 alerts, < 5 open Phase 4a bugs
- [ ] Run progress review (lifecycle/13-progress-review.md) — verify 10.0 / A+
- [ ] Commit + push + tag `v0.4.0-a-plus`

---

## TOTAL ITEM COUNT

| Tier | Items | Sessions | Focus |
|------|-------|----------|-------|
| 0: Stop bleeding | 5 | 0.5 | Build fixes |
| 1: Gate closure | 4 | 0.5 | Phase 4a gate |
| 2: Correctness | 23 | 2 | Lean + Kani + CI-FERR |
| 3: Verification depth | 18 | 2 | Stateright + Integration + Type |
| 4: Code quality | 6 | 0.5 | Toolchain pinning |
| 5: Performance | 11 | 1 | Assertions + benchmarks |
| 6: Durability | 6 | 1 | Crash scenarios |
| 7: Completeness | 3 | 0.5 | 010/017 completion |
| 8: Architecture | 7 | 0.5 | Trait documentation |
| 9: Ergonomics | 6 | 0.5 | Error + usage docs |
| 10: Axiological | 4 | 0.5 | Traceability audit |
| 11: Process | 9 | 1.5 | Cleanroom review + hygiene |
| **TOTAL** | **102 items** | **9 sessions** | |

---

## WHAT 10.0 LOOKS LIKE WHEN WE'RE DONE

```
$ cargo check --workspace --all-targets
    Finished dev [unoptimized + debuginfo] target(s)

$ cargo clippy --workspace --all-targets -- -D warnings
    Finished dev [unoptimized + debuginfo] target(s)

$ cargo clippy --workspace --lib -- -D warnings -D clippy::unwrap_used -D clippy::expect_used -D clippy::panic
    Finished dev [unoptimized + debuginfo] target(s)

$ cargo fmt --all -- --check
    (no output — all formatted)

$ PROPTEST_CASES=10000 cargo test --workspace --release
    test result: ok. 320+ passed; 0 failed; 0 ignored

$ cd ferratomic-verify/lean && lake build
    Build completed successfully (0 sorry)

$ bv --robot-alerts
    0 alerts

$ bv --robot-triage | jq '.triage.project_health.counts.by_status.open'
    < 5 (Phase 4a)

Every INV-FERR-001..032: Lean ✓ + proptest 10K ✓ + Kani ✓ + (Stateright or N/A) ✓ + Integration ✓
Every public item: documented with INV-FERR reference
Every trait: contract documented
Every ADR: justified against GOALS.md value hierarchy
Every commit: passes all 5 quality gates (pre-commit hook)
Phase 4a gate: CLOSED (v0.4.0-gate tag)
Cleanroom review: PASSED and documented
```

That is 10.0. Zero defects. Lab-grade. NASA-grade Rust.
